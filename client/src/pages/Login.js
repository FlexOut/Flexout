import { useState } from 'react'
import { login } from '../services/auth';
import LoginComponent from '../components/Log-Signup/Login'

export default function Login(props) {

	return (
			<LoginComponent />		
	)
}